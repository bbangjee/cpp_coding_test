#include <bits/stdc++.h>

using namespace std;
/* 백준 2293 동전 1 */
int n, k;
vector<int> coins;

void dp() {
        
}


int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> n >> k;
    for (int i = 0; i < n; i++) {
        int temp;
        cin >> temp;
        coins.push_back(temp);
    }

    return 0;
}